# Welcome to Jekyll!
#
# This config file is meant for settings that affect your whole blog, values
# which you are expected to set up once and rarely edit after that. If you find
# yourself editing this file very often, consider using Jekyll's data files
# feature for the data you need to update frequently.
#
# For technical reasons, this file is *NOT* reloaded automatically when you use
# 'bundle exec jekyll serve'. If you change this file, please restart the server process.

# Site settings
# These are used to personalize your new site. If you look in the HTML files,
# you will see them accessed via {{ site.title }}, {{ site.email }}, and so on.
# You can create any custom variable you would like, and they will be accessible
# in the templates via {{ site.myvariable }}.
title: dapeng-soa
author: dapeng
email: dapengsoa@gmail.com
description: >- # this means to ignore newlines until "baseurl:"
  dapeng 为互联网应用打造的SOA框架，提供一个开发简单、核心高效、方便运维、工具丰富的微服务开发支持。
baseurl: "" # the subpath of your site, e.g. /blog
url: "" # the base hostname & protocol for your site, e.g. http://example.com
twitter_username: dapeng-soa
github_username: 'dapeng-soa'
github_repo: https://github.com/dapeng-soa/dapeng-soa.github.io
color_style: "light" #dark or light
rss: true
lang: zh-Hans
google_analytics: UA-NNNNNNNN-N
show_excerpts: true
minima:
  date_format: "%Y-%m-%d"
paginate: 3 #博客分页文章数
paginate_path: _posts:num

# Build settings
markdown: kramdown
highlighter: rouge
theme: minima
plugins:
  - jekyll-feed
  - jekyll-paginate

# Exclude from processing.
# The following items will not be processed, by default. Create a custom list
# to override the default setting.
exclude:
  - Gemfile
  - Gemfile.lock
  - node_modules
  - vendor/bundle/
  - vendor/cache/
  - vendor/gems/
  - vendor/ruby/
  
include:
  - docs  
